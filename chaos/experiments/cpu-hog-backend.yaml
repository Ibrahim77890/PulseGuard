apiVersion: chaos-mesh.org/v1alpha1
kind: StressChaos
metadata:
  name: backend-cpu-hog
  namespace: chaos-testing
spec:
  mode: one
  selector:
    namespaces:
      - backend
    labelSelectors:
      app: backend
  stressors:
    cpu:
      workers: 2
      load: 90
  duration: "120s"
